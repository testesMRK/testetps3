name: ci

on:
  pull_request:
    branches: [main, develop, 'release/**']

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup
        run: echo "🔧 Configurar ambiente aqui"

      - name: Build
        run: echo "🏗️ Build do projeto aqui"

      - name: Test
        run: echo "🧪 Testes aqui"

  code-review:
    name: code-review
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Lint
        run: echo "📝 Lint / análise estática aqui"

      - name: Security scan
        run: echo "🔒 Verificação de segurança aqui"
